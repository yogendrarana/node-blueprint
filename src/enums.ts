export enum FrameworkEnum {
    express = "express",
    fastify = "fastify"
}

export enum DatabaseEnum {
    mysql = "mysql",
    postgres = "postgres"
}

export enum OrmEnum {
    prisma = "prisma",
    drizzle = "drizzle"
}

export enum AuthEnum {
    yes = "yes",
    no = "no"
}
