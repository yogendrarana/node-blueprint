export enum FrameworkEnum {
    express = "express",
    fastify = "fastify",
    koa = "koa"
}

export enum DatabaseEnum {
    mysql = "mysql",
    postgres = "postgres",
    mongodb = "mongodb"
}

export enum OrmEnum {
    prisma = "prisma",
    drizzle = "drizzle",
    mongoose = "mongoose"
}

export enum AuthEnum {
    yes = "yes",
    no = "no"
}

export enum CommandType {
    create = "create"
}
